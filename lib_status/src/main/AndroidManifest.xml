<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:enableOnBackInvokedCallback="true"
        tools:ignore="UnusedAttribute">
        <activity
            android:name=".TestAppStatusActivity"
            android:exported="true"
            android:configChanges="screenSize|keyboard|keyboardHidden|layoutDirection|smallestScreenSize|orientation|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="darcyscheme" android:host="com.darcy.message" android:path="/status"/>
            </intent-filter>
        </activity>
        <service android:name=".service.CustomNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>

            <meta-data
                android:name="android.service.notification.NotificationListenerService"
                android:value="MyNotificationListenerService" />
        </service>


        <receiver android:name="com.darcy.message.lib_app_status.receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>
    </application>

</manifest>